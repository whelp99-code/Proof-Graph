---
description: Show the current ProofGraph run status
---

Call `proofgraph_status`. Use `$ARGUMENTS` as the run ID when provided; otherwise use the active run. Render ready/running/failed nodes and pending approvals without inventing state.
