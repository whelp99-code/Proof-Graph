---
description: Compile and execute a ProofGraph development graph
---

Call the `proofgraph_run` tool with the following objective exactly as supplied by the user:

$ARGUMENTS

After starting, show the returned run ID, current state, selected template, pending approval, and the next safe operator action. Do not bypass ProofGraph tool-policy or approval decisions.
