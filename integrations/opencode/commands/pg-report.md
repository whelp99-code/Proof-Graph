---
description: Show a ProofGraph run report
---

Call `proofgraph_report`. Use `$ARGUMENTS` as the run ID when provided; otherwise use the active run. Preserve failed, blocked, partial, and unverified items.
